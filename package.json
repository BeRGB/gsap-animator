{
  "name": "gsap-animator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy:html-img && npm run copy:lottie && npm run spa:404",
    "copy:html-img": "mkdir -p dist/src && cp -R src/img dist/src/img || true",
    "copy:lottie": "test -d src/lottie && (mkdir -p dist/src && cp -R src/lottie dist/src/lottie) || true",
    "spa:404": "cp dist/index.html dist/404.html || true",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.2",
    "vite-plugin-static-copy": "^3.1.2"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "lottie-web": "^5.13.0",
    "three": "^0.179.1"
  }
}
